# Профилирование и оптимизация view-функций

## Обзор

В проекте реализовано профилирование view-функций через `tracemalloc` для анализа использования памяти и производительности. Профилируются три основные view-функции:
1. **PostsList** - список всех постов
2. **PostDetail** - детальная страница поста
3. **edit_post** - редактирование поста

**Доступны два режима работы:**
- **Профилирование** - показывает текущие метрики производительности
- **Сравнение** - сравнивает производительность до и после оптимизаций, показывая конкретные улучшения в %

## Структура файлов

- `news_portal/profiling.py` - утилита для профилирования функций через tracemalloc
- `manage_profiling.py` - standalone скрипт для профилирования (работает без запущенного сервера)
- `tests/profiling_tests.py` - тесты для детального профилирования
- `tests/load_tests.py` - нагрузочные тесты

## Использование

### 1. Standalone профилирование (БЕЗ запущенного сервера)

Самый простой способ запустить профилирование - использовать скрипт `manage_profiling.py`:

```bash
python manage_profiling.py
```

Этот скрипт:
- Автоматически настраивает Django окружение
- Создает тестовые данные (если их нет)
- Выполняет профилирование всех трех view-функций
- Выводит детальную информацию о:
  - Времени выполнения
  - Использовании памяти (текущее и пиковое)
  - Количестве SQL запросов
  - Времени выполнения SQL запросов
  - Топ 5 строк по использованию памяти

**Преимущества:**
- Не требует запущенного сервера Django
- Работает изолированно
- Автоматически создает тестовые данные
- Подробный вывод результатов

### 2. Запуск тестов профилирования

Для детального профилирования каждой view-функции:

```bash
python manage.py test tests.profiling_tests -v 2
```

Эти тесты проверяют:
- Корректность работы view-функций
- Количество SQL запросов (должно быть оптимизировано)
- Использование памяти
- Время выполнения

### 3. Запуск нагрузочных тестов

Для проверки производительности под нагрузкой:

```bash
python manage.py test tests.load_tests -v 2
```

Нагрузочные тесты выполняют:
- `test_posts_list_load` - 20 запросов к списку постов
- `test_post_detail_load` - 20 запросов к детальной странице
- `test_edit_post_load` - 10 запросов к странице редактирования
- `test_concurrent_requests` - 30 смешанных запросов (разные типы)

Каждый тест проверяет:
- Среднее время отклика
- Использование памяти
- Количество успешных запросов

### 4. Сравнение производительности до и после оптимизации

Для наглядного сравнения улучшений:

```bash
python manage_profiling_comparison.py
```

Этот скрипт:
- Тестирует каждую view-функцию **без оптимизаций** (без `select_related`/`prefetch_related`)
- Тестирует каждую view-функцию **с оптимизациями**
- Выводит детальное сравнение:
  - Время выполнения (улучшение в %)
  - Количество SQL запросов (уменьшение в %)
  - Использование памяти (улучшение в %)
  - Общая оценка улучшения

**Пример вывода:**
```
================================================================================
СРАВНЕНИЕ: PostsList
================================================================================

⏱ ВРЕМЯ ВЫПОЛНЕНИЯ:
  Без оптимизации:  0.4500 сек
  С оптимизацией:   0.2300 сек
  Улучшение:        +48.9% (↑)

🗄 SQL ЗАПРОСЫ:
  Без оптимизации:  25 запросов (0.1500 сек)
  С оптимизацией:   12 запросов (0.0800 сек)
  Улучшение:        +52.0% запросов меньше (↑)

ПАМЯТЬ (пиковое использование):
  Без оптимизации:  5.20 MB
  С оптимизацией:   4.65 MB
  Улучшение:        +10.6% (↑)

ИТОГОВАЯ ОЦЕНКА:
  Отличное улучшение: 37.2%
```

### 5. Использование декоратора профилирования

Для профилирования собственных функций можно использовать декоратор из `news_portal/profiling.py`:

```python
from news_portal.profiling import profile_view

@profile_view
def my_function():
    # ваш код
    pass
```

## Выполненные оптимизации

### PostsList

1. **Оптимизация queryset:**
   - Добавлен `select_related('author', 'author__user')` - загружает автора и пользователя одним запросом
   - Добавлен `prefetch_related('category')` - загружает категории эффективно

2. **Оптимизация формы подписок:**
   - Используется `select_related('category')` при загрузке подписок пользователя

3. **Оптимизация обновления подписок:**
   - Используется `bulk_create` и `bulk_delete` вместо циклов для массовых операций

### PostDetail

1. **Оптимизация queryset:**
   - Добавлен `select_related('author', 'author__user')`
   - Добавлен `prefetch_related('category')`

2. **Оптимизация комментариев:**
   - Используется `select_related('user')` при загрузке комментариев

3. **Кэширование поста:**
   - Пост кэшируется на 300 секунд для уменьшения запросов к БД

### edit_post

1. **Оптимизация загрузки поста:**
   - Используется `select_related('author', 'author__user')`
   - Используется `prefetch_related('category')`

2. **Оптимизация категорий:**
   - Категории загружаются один раз через prefetch_related

3. **Кэширование:**
   - Кэш поста удаляется после обновления

## Ожидаемые результаты

После оптимизации ожидается:
- **Уменьшение количества SQL запросов на 40-60%**
- **Улучшение времени отклика на 30-50%**
- **Снижение использования памяти** за счет оптимизации запросов

### Примерные метрики

**PostsList:**
- SQL запросов: до 15 (раньше могло быть 30+)
- Время отклика: < 200ms
- Использование памяти: < 50 MB

**PostDetail:**
- SQL запросов: до 10 (раньше могло быть 20+)
- Время отклика: < 150ms
- Использование памяти: < 40 MB

**edit_post:**
- SQL запросов: до 12 (раньше могло быть 25+)
- Время отклика: < 300ms
- Использование памяти: < 45 MB

## Требования

Для работы профилирования необходимо:
- Python 3.8+
- Django 5.0+
- tracemalloc (встроен в Python 3.4+)

Для запуска тестов требуется:
- Активная база данных SQLite (по умолчанию)
- Тестовые данные (создаются автоматически)